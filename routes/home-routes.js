const router = require('express').Router();
const { MODELS HERE } = require('./PATHTO MODELS');
const userAuth = require('PATHTOAUTH');
// do we need a timestamp helper here for post times?

// GET for homepage
router.get('/', (req, res) => {

});

// GET user login
router.get('/login', (req, res) => {
    
})